<h1><%= t('groups.index.heading') %></h1>
<ul>
  <% Group.all.each do |group| %>
    <li><%= link_to(group.title, group_path(group)) %>
    <% if group.description.present? %>
      <p><%= group.description %></p>
    <% end %>
  <% end %>
</ul>
